DATABASE_URL="postgresql://mozporn:mozporn_dev_password@localhost:5432/mozporn?schema=public"
AUTH_SECRET="change-this-to-a-long-random-secret-at-least-32-characters"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
ADMIN_EMAIL="admin@mozporn.local"
ADMIN_PASSWORD="replace-with-a-strong-local-password"

# Local development only: password reset links are printed in the server console.
# In production, connect a transactional email provider and never expose reset tokens.
